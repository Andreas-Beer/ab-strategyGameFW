import { expect } from "chai";

describe("observer.ts", () => {
  describe("addObserver()", () => {
    it("should add the function to the given List", () => {
      expect("1").eq("2");
    });
  });
});
